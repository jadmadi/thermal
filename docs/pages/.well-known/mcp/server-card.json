{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/server-card.json",
  "serverInfo": {
    "name": "thermal",
    "version": "0.13.0",
    "title": "Thermal AI Coding Activity & Leaderboard MCP Server",
    "description": "High-performance terminal contribution heatmap, streak tracker, and telemetry leaderboard tool for AI coding assistants.",
    "homepage": "https://thermal.jadmadi.net",
    "repository": "https://github.com/jadmadi/thermal"
  },
  "transport": {
    "type": "stdio",
    "command": "thermal",
    "args": []
  },
  "capabilities": {
    "tools": [
      {
        "name": "thermal_leaderboard",
        "description": "Retrieve the AI coding assistant leaderboard with disjoint token metrics (input, output, cache read/write, reasoning) and cost estimations across Claude Code, Devin, OpenCode, Codex, and 11 other tools.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "sort": {
              "type": "string",
              "enum": ["streak", "tokens", "cost"],
              "description": "Field to sort the leaderboard by"
            },
            "json": {
              "type": "boolean",
              "description": "Return structured JSON payload"
            }
          }
        }
      },
      {
        "name": "thermal_streaks",
        "description": "Retrieve daily contribution activity, current active streaks, and longest historical streak across all local AI coding assistants.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "tool": {
              "type": "string",
              "description": "Filter by tool name or 'auto'"
            }
          }
        }
      },
      {
        "name": "thermal_projects",
        "description": "Retrieve ranked project directory attributions with normalized git root aggregation and token contributions.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "sort": {
              "type": "string",
              "enum": ["tokens", "cost", "days", "recent"],
              "description": "Field to sort project breakdown by"
            },
            "top": {
              "type": "integer",
              "description": "Limit returned projects count"
            }
          }
        }
      },
      {
        "name": "thermal_models",
        "description": "Retrieve model distribution, disjoint tokens, and price catalog estimates.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "sort": {
              "type": "string",
              "enum": ["tokens", "cost"],
              "description": "Field to sort models breakdown by"
            }
          }
        }
      }
    ]
  }
}
